######### Export functions to users.
export(autosimi, compas, 
      part.m.tree, part.p.tree, 
      select.window, 
      treeNodf, treeNodfTest,
      dis.goodall, dis.chao, dis.nness, dis.nness.find.m
)


######### Import function from other packages.
import(vegan, ape, picante, graphics, stats, utils)# entire packages imported.
#importFrom(vegan, nestednodf) #only nestednodf from vegan is imported.
#importFrom(gmp, chooseZ)
#importFrom(gmp, asNumeric)


######### S3 methods
S3method(print, treeNodf) # one method per line
S3method(print, treeNodfTest)
